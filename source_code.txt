"""
	Código de prueba para 
	# Análisis Sintáctico #

"""
#Calcular factorial
def calcularFac(num):
    num_aux = 0
    if (num < 1):
        num_aux = -1
    else:
        num_aux = num * (calcularFac(num--1))
    return num_aux

ventas = [100,200,300,400,500,-3.14]

Caracter = '%'

#promedio de elementos
def promedio(cualquier_arreglo):
    tam = len(cualquier_arreglo)
    resultado=0
    #ciclo para recorrer arreglo
    cont = 0
    sumatoria = 0
    while (cont <= (tam-1)):
        sumatoria = sumatoria + cualquier_arreglo[cont]
        cont = cont + 1
    if tam > 0:
        resultado = sumatoria / tam
    else:					# Se agrega un else con una asignación a resultado
	resultado = 0
    return resultado				#Parser Error - line 31:4 mismatched input 'return' expecting 'else' # Todo if lleva else
    

def calcularpromedioventas():
    prom = promedio(len(ventas))
    print("El promedio de las ventas es: ")
    print("\n")
    '''print '\n'					#Scanner Error - line 36:11 token recognition error at: '\' # Se deben usar comillas dobles, comillas simples toma solamente un char y el \ no está definido en la gramática # Parser error - line 36:14 mismatched input ... # El print debe llevar parentesis
    	print(prom) #posible error de indentación'''

print("Calculo del Factorial:%$ " + calcularFac(6))
print calcularpromedioventas(hola,10)
